# Database
DATABASE_URL=postgresql://mmf:mmf@localhost:5432/mmf

# Server
PORT=3001
NODE_ENV=development

# Auth — signing key for JWT tokens (any string works locally)
JWT_SECRET=local-dev-jwt-secret
