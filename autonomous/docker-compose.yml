services:
  agent:
    build: .
    env_file: .env
    environment:
      - MILESTONE_NUMBER=${MILESTONE_NUMBER}
    volumes:
      - ./.mcp.json:/home/agent/.mcp.json:ro
      - ./screenshots:/screenshots
    mem_limit: 4g
    cpus: 2
