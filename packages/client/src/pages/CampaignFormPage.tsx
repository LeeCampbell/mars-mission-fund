export interface CampaignFormPageProps {
  campaignId?: string
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function CampaignFormPage(_props: CampaignFormPageProps) {
  return <div>Campaign Form</div>
}
