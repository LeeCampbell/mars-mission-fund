# Tasks: Issue #115 — Reviewer and Admin Campaign Management UI

Brief: plan/ready/brief.md

## Checklist

- [x] TASK-01: Backend — `markNotificationRead` query + `PATCH /:id/read` endpoint
  - **Goal**: Add the server-side ability to mark a notification as read via REST
  - **Details**:
    - In `packages/server/src/campaigns/queries.ts`, add `markNotificationRead(pool, notificationId, userId)` following the pattern of `createNotification` / `getNotificationsForUser`. The query must `UPDATE notifications SET read = true WHERE id = $1 AND user_id = $2 RETURNING *` to enforce ownership.
    - In `packages/server/src/notifications/routes.ts`, add `PATCH /:id/read` (authenticated). Validate `:id` is a UUID. Call `markNotificationRead`. If no row returned (not found or wrong user), return 403. On success, return 200 with `{ data: updatedNotification }`.
  - **Files**:
    - `packages/server/src/campaigns/queries.ts`
    - `packages/server/src/notifications/routes.ts`
  - **Verify**: `npm run build -w @mmf/shared && npx tsc --noEmit -p packages/server/tsconfig.json` passes; `npm run test:coverage` passes
  - **Brief ref**: Section 1 — Backend

- [x] TASK-02: Client API additions — notifications + campaign admin actions
  - **Goal**: Add all missing client-side API functions consumed by new components
  - **Details**:
    - In `packages/client/src/api/notifications.ts`, add `markNotificationAsRead(id: string): Promise<Notification>` — calls `PATCH /v1/notifications/:id/read`, validates response with `NotificationSchema`.
    - In `packages/client/src/api/campaigns.ts`, add:
      - `verifyMilestone(campaignId: string, milestoneId: string): Promise<CampaignDetail>` → `POST /v1/campaigns/:id/milestones/:mid/verify`
      - `returnMilestone(campaignId: string, milestoneId: string, feedback: string): Promise<CampaignDetail>` → `POST /v1/campaigns/:id/milestones/:mid/return`
      - `approveCancellation(campaignId: string): Promise<CampaignDetail>` → `POST /v1/campaigns/:id/approve-cancel`
      - `denyCancellation(campaignId: string): Promise<CampaignDetail>` → `POST /v1/campaigns/:id/deny-cancel` (check if endpoint exists on server first; if not, add a TODO comment and skip for now)
    - Follow existing patterns: use `apiFetch`, validate with Zod schemas from `@mmf/shared`.
  - **Files**:
    - `packages/client/src/api/notifications.ts`
    - `packages/client/src/api/campaigns.ts`
  - **Verify**: `npx tsc -b --noEmit` passes; functions are exported and typed correctly
  - **Brief ref**: Sections 1, 4

- [x] TASK-03: Route rename + ReviewQueuePage fix + ReviewDetailPage
  - **Goal**: Rename reviewer queue route to `/review`, update post-claim navigation, and create the review detail page
  - **Details**:
    - In `packages/client/src/App.tsx`:
      - Change the route path for `ReviewQueuePage` from `/review-queue` to `/review`.
      - Add a new lazy-loaded route `/review/:id` → `ReviewDetailPage` protected with `requireReviewer`.
    - In `packages/client/src/pages/ReviewQueuePage.tsx`, change post-claim navigation from `/campaigns/:id` to `/review/:id`.
    - Create `packages/client/src/pages/ReviewDetailPage.tsx`:
      - Protected with `requireReviewer` (handled by route, but the page can also check).
      - Uses `useCampaign(id)` hook (same as `CampaignDetailPage`).
      - Shows: campaign header (title, category, status, hero image), full description and alignment statement, milestones section with verification criteria, team section and stretch goals.
      - Renders `ReviewActionsPanel` (already exists in `packages/client/src/components/campaigns/ReviewActionsPanel.tsx`).
      - Includes a "← Back to Review Queue" link to `/review`.
      - No creator/backer UI elements (no donate button, no post update form, no submit evidence form).
      - Handle loading state (spinner/skeleton) and error state.
  - **Files**:
    - `packages/client/src/App.tsx`
    - `packages/client/src/pages/ReviewQueuePage.tsx`
    - `packages/client/src/pages/ReviewDetailPage.tsx` (create)
  - **Verify**: `npx tsc -b --noEmit` passes; navigating to `/review` shows queue, claiming navigates to `/review/:id`
  - **Brief ref**: Sections 2, 3

- [x] TASK-04: `useNotifications` hook
  - **Goal**: Create the React Query hook that centralises notification data-fetching and mutation
  - **Details**:
    - Create `packages/client/src/hooks/useNotifications.ts`.
    - Uses `useQuery` from `@tanstack/react-query` with `queryKey: ['notifications']` and `queryFn: fetchNotifications`.
    - Uses `useMutation` wrapping `markNotificationAsRead(id)` with `onSuccess: () => queryClient.invalidateQueries({ queryKey: ['notifications'] })`.
    - Returns `{ notifications: Notification[], unreadCount: number, markAsRead: (id: string) => void, isLoading: boolean }`.
    - `unreadCount` is `notifications.filter(n => !n.read).length`.
    - Only enabled when user is authenticated (check via `useAuth`).
  - **Files**:
    - `packages/client/src/hooks/useNotifications.ts` (create)
  - **Verify**: `npx tsc -b --noEmit` passes; hook exports correct types
  - **Brief ref**: Section 5

- [x] TASK-05: NotificationsPage + `/notifications` route
  - **Goal**: Create the full notifications list page and wire its route
  - **Details**:
    - In `packages/client/src/App.tsx`, add a lazy-loaded route `/notifications` → `NotificationsPage` protected for any authenticated user.
    - Create `packages/client/src/pages/NotificationsPage.tsx`:
      - Uses `useNotifications()` hook.
      - Lists all notifications newest-first.
      - Each row shows: title, message, relative timestamp (use `date-fns` if already in project, otherwise format manually), unread/read visual distinction (e.g. bold title or coloured left-border for unread).
      - Each unread notification has a "Mark as read" button that calls `markAsRead(id)`.
      - Clicking the notification row navigates to the related campaign (`/campaigns/:campaignId`) if `campaignId` is set.
      - Empty state: friendly message when no notifications.
      - Loading state while fetching.
  - **Files**:
    - `packages/client/src/App.tsx`
    - `packages/client/src/pages/NotificationsPage.tsx` (create)
  - **Verify**: `npx tsc -b --noEmit` passes; page renders at `/notifications`
  - **Brief ref**: Section 5

- [x] TASK-06: `NotificationBell` component
  - **Goal**: Add a notification bell with unread badge and dropdown to the header
  - **Details**:
    - Create `packages/client/src/components/NotificationBell.tsx`:
      - Uses `useNotifications()` hook.
      - Renders a bell SVG icon button.
      - Shows an absolute-positioned badge with `unreadCount` when count > 0; hidden when 0.
      - On click, toggles a dropdown panel anchored below the bell.
      - Dropdown shows the most recent 10 notifications (title, short message excerpt, relative time).
      - Unread notifications are visually distinct from read ones.
      - Each notification row calls `markAsRead(id)` and navigates to `/campaigns/:campaignId` on click.
      - Includes a "View all notifications" link to `/notifications` at the bottom of the dropdown.
      - Clicking outside the dropdown closes it (use a click-outside effect or `onBlur`).
      - Only rendered for authenticated users (caller will conditionally render in Header).
  - **Files**:
    - `packages/client/src/components/NotificationBell.tsx` (create)
  - **Verify**: `npx tsc -b --noEmit` passes; component renders without errors
  - **Brief ref**: Section 5

- [ ] TASK-07: `AdminActionsPanel` + `AdminCampaignsPage` + CampaignDetailPage integration
  - **Goal**: Build admin milestone verification and cancellation approval UI
  - **Details**:
    - Create `packages/client/src/components/campaigns/AdminActionsPanel.tsx`:
      - Accepts `campaign: CampaignDetail` and `onActionComplete: () => void` props.
      - **Milestone verification panel**: renders when `campaign.status === 'Settlement'` and user role is `Administrator` or `SuperAdministrator`. Lists milestones with `status === 'Submitted'`, showing evidence description and URL. Each milestone has a "Verify" button (calls `verifyMilestone`) and a "Return" button that reveals a feedback textarea and confirm button (calls `returnMilestone`). On success call `onActionComplete()`.
      - **Cancellation approval panel**: renders when `campaign.cancellationRequestedAt` is truthy and user is Admin. Shows two buttons: "Approve Cancellation" (calls `approveCancellation`) and "Deny Cancellation" (calls `denyCancellation` if the endpoint exists, otherwise show a disabled/TODO state). On success call `onActionComplete()`.
      - If `CampaignDetail` type does not include `cancellationRequestedAt`, check `packages/shared/src/campaign.ts` and `CampaignDetailSchema` — if missing, add the field there and in the server schema as well.
      - Use `useMutation` for each action with query invalidation on success.
    - In `packages/client/src/pages/CampaignDetailPage.tsx`, import and render `AdminActionsPanel` in the right sidebar following the same conditional-render pattern as `ReviewActionsPanel`.
    - Create `packages/client/src/pages/AdminCampaignsPage.tsx` at `/admin/campaigns`:
      - Protected with `requireAdmin` (same HOC used by AdminUsersPage).
      - Fetches campaigns with `status=Settlement` using `useCampaigns({ status: 'Settlement' })` or a direct API call.
      - Fetches campaigns with pending cancellation requests. If `CampaignSummary` has `cancellationRequestedAt`, filter client-side. Otherwise fetch `status=Live` and note the limitation.
      - Displays two sections: "Awaiting Milestone Verification" and "Pending Cancellation Requests".
      - Each campaign row links to `/campaigns/:id`.
      - Empty states for each section.
    - In `packages/client/src/App.tsx`, add a lazy-loaded route `/admin/campaigns` → `AdminCampaignsPage` protected with `requireAdmin`.
  - **Files**:
    - `packages/client/src/components/campaigns/AdminActionsPanel.tsx` (create)
    - `packages/client/src/pages/AdminCampaignsPage.tsx` (create)
    - `packages/client/src/pages/CampaignDetailPage.tsx`
    - `packages/client/src/App.tsx`
    - `packages/shared/src/campaign.ts` (if `cancellationRequestedAt` is missing from `CampaignDetailSchema`)
  - **Verify**: `npx tsc -b --noEmit` passes; pages render; panels appear on correct campaign detail pages
  - **Brief ref**: Section 4

- [ ] TASK-08: Header updates — `NotificationBell` + role-based nav links
  - **Goal**: Wire all new navigation elements into the header
  - **Details**:
    - In `packages/client/src/components/Header.tsx`:
      - For users with role `'Reviewer'`: add a "Review" nav link to `/review` in both desktop and mobile nav sections.
      - For users with role `'Administrator'` or `'SuperAdministrator'`: add an "Admin" nav link to `/admin/campaigns` in both desktop and mobile nav sections (the existing Admin badge can remain as is or be removed if redundant — prefer keeping both for clarity).
      - For all authenticated users: render `<NotificationBell />` in the header (right side, near the user display name / logout button).
  - **Files**:
    - `packages/client/src/components/Header.tsx`
  - **Verify**: `npx tsc -b --noEmit` passes; reviewer sees "Review" link; admin sees "Admin" link; bell appears for logged-in users
  - **Brief ref**: Section 6

- [ ] TASK-09: Unit tests for all new components and hooks
  - **Goal**: Achieve ≥80% coverage threshold with tests for all new code
  - **Details**:
    - `packages/client/src/pages/ReviewDetailPage.test.tsx`:
      - Mock `useCampaign` to return a campaign in `Under Review` state assigned to the test user.
      - Assert: campaign title renders, `ReviewActionsPanel` renders, back-link to `/review` renders.
    - `packages/client/src/components/NotificationBell.test.tsx`:
      - Mock `useNotifications` returning 3 notifications (2 unread).
      - Assert: badge shows "2", clicking bell opens dropdown with notification titles, "View all" link present.
      - Assert: clicking a notification calls `markAsRead`.
    - `packages/client/src/pages/NotificationsPage.test.tsx`:
      - Mock `useNotifications` returning mixed read/unread notifications.
      - Assert: all notifications render, unread ones show "Mark as read" button, clicking it calls `markAsRead`.
      - Assert: empty state renders when `notifications` is empty.
    - `packages/client/src/components/campaigns/AdminActionsPanel.test.tsx`:
      - Mock `useMutation` (or the API functions via `vi.mock`).
      - Assert: milestone verification panel renders for Settlement campaign + admin user.
      - Assert: cancellation panel renders for campaign with `cancellationRequestedAt` set + admin user.
      - Assert: neither panel renders for non-admin user.
    - Follow existing test patterns (see `ReviewActionsPanel.test.tsx` if it exists, otherwise follow `CampaignDetailPage.test.tsx` patterns).
  - **Files**:
    - `packages/client/src/pages/ReviewDetailPage.test.tsx` (create)
    - `packages/client/src/components/NotificationBell.test.tsx` (create)
    - `packages/client/src/pages/NotificationsPage.test.tsx` (create)
    - `packages/client/src/components/campaigns/AdminActionsPanel.test.tsx` (create)
  - **Verify**: `npm run test:coverage` passes with ≥80% threshold
  - **Brief ref**: Section 7

- [ ] TASK-10: Write E2E tests
  - **Goal**: Create Playwright E2E tests covering the user flows described in the brief
  - **Details**: Create or update files in `e2e/`. Follow patterns in existing tests (`e2e/auth.spec.ts`, `e2e/campaigns.spec.ts`). Use Playwright Test API. Tests must pass against the running local stack.
    - **Reviewer flow**: Log in as reviewer → navigate to `/review` → claim a campaign → assert navigation to `/review/:id` → approve the campaign.
    - **Admin flow**: Log in as admin → navigate to `/admin/campaigns` → assert page renders with expected sections (milestone verification, cancellation requests).
    - **Notification flow**: Log in as any authenticated user → assert notification bell is visible in header → click bell → assert dropdown opens with notification entries → click a notification → assert navigation to campaign detail → navigate to `/notifications` → assert full list renders.
  - **Files**: `e2e/reviewer.spec.ts`, `e2e/admin-campaigns.spec.ts`, `e2e/notifications.spec.ts` (or combined into existing files if patterns allow)
  - **Verify**: Run `npm run test:e2e` — all tests pass (existing + new)
  - **Brief ref**: Verification section
